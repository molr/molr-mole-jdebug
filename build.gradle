apply plugin: "java"

subprojects {
    apply plugin: "java"
}

project(":agent") {
    dependencies {
        compile project(":inspector")
    }
}

project(":controller") {
    dependencies {
        compile project(":agent")
    }
}

project(":gui") {
    dependencies {
        compile project(":inspector")
    }
}
